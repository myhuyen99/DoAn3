@model IEnumerable<DoAn3.Models.Diadiemdulich>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DddlTen)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DddlTen)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DddlMa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DddlMa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DddlMa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
