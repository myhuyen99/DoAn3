@model IEnumerable<DoAn3.Models.Taikhoan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TkEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TkMatkhau)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TkHoten)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TkEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TkMatkhau)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TkHoten)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TkMa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TkMa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TkMa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
