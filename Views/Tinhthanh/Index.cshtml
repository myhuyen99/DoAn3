@model IEnumerable<DoAn3.Models.Tinhthanh>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TtTen)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TtTen)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TtMa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TtMa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TtMa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
