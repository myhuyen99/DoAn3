@model IEnumerable<DoAn3.Models.Quantri>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QtEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QtMatkhau)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QtEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QtMatkhau)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QtMa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QtMa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QtMa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
